name: atomicdataMB

channels:
    - defaults

dependencies:
    - python>=3.7
    - setuptools
    - numpy
    - pip:
      - sphinx
      - sphinx-automodapi
      - stsci_rtd_theme
      - periodictable
